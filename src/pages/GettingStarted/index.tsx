import React from "react";

const GettingStarted: React.FC = () => {
  return (
    <pre>
      Not sure if we should point to devcontainer yet , or just say "wait a bit"
      , or something else.
    </pre>
  );
};

export default GettingStarted;
